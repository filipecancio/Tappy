
/*Retorna true se for objeto e false se não for
* @param {any} valor
* @return {boolean} resultado
* */
const isObject = (obj) => typeof obj ==='object';

/*Converte arrray em object
* @param chave
* @param valor
* @param objeto
* @return o bojeto*/
const arrayToObject = (key,value,obj={})=>{
    return obj;
}